use crate::{
    iam::{
        entity::{role::Role, user::User},
        value_object::user_id::UserId,
    },
    shared::event_util::UpdatedEvent,
};

#[derive(Debug, Clone)]
pub enum {{module | pascal_case}}Event {
    {{entity | pluralize}}Created { items: Vec<{{entity | pascal_case}}> },
    {{entity | pluralize}}Updated { items: Vec<UpdatedEvent<{{entity | pascal_case}}>> },
    {{entity | pluralize}}Deleted { items: Vec<{{entity | pascal_case}}> },

}
