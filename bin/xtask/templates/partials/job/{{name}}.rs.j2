use sched_kit::error::Result;
use nject::injectable;
use sched_kit::ScheduledJob;
use tracing::info;

#[derive(Clone)]
#[injectable]
pub struct {{name | pascal_case}} {
}

impl ScheduledJob for {{name | pascal_case}} {
    const EXPR: &'static str = "every 3 seconds";
    const NAME: &'static str = "{{name | pascal_case}}";

    async fn run(&self) -> Result<()> {
        info!("{{name | pascal_case}} runing");
        Ok(())
    }
}
