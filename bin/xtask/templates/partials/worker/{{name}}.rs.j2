use anyhow::Result;
use bg_worker_kit::{Worker, error::WorkerError};
use nject::injectable;
use serde::{Deserialize, Serialize};

#[derive(Clone, Serialize, Deserialize)]
pub struct {{name | pascal_case}}Params {
    // Define job parameters here
}

#[derive(Clone)]
#[injectable]
pub struct {{name | pascal_case}};

impl Worker for {{name | pascal_case}} {
    type Params = {{name | pascal_case}}Params;

    async fn run(&self, _params: Self::Params) -> Result<(), WorkerError> {

        Ok(())
    }
}
