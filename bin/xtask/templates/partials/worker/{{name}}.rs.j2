use anyhow::Result;
use bg_worker_kit::{JobRunner, error::RunnerError};
use nject::injectable;
use serde::{Deserialize, Serialize};

#[derive(Clone, Serialize, Deserialize)]
pub struct {{name | pascal_case}}Params {
    // Define job parameters here
}

#[derive(Clone)]
#[injectable]
pub struct {{name | pascal_case}};

impl JobRunner for {{name | pascal_case}} {
    type Params = {{name | pascal_case}}Params;

    async fn run(&self, _params: Self::Params) -> Result<(), RunnerError> {

        Ok(())
    }
}
