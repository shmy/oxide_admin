use bg_worker_kit::{Worker, error::Result};
use bon::Builder;
use nject::injectable;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, Builder)]
pub struct {{name | pascal_case}}Params {
    // Define worker parameters here
}

#[derive(Clone)]
#[injectable]
pub struct {{name | pascal_case}};

impl Worker for {{name | pascal_case}} {
    const KIND: &'static str = "{{name}}";
    type Params = {{name | pascal_case}}Params;

    async fn run(&self, _params: Self::Params) -> Result<()> {

        Ok(())
    }
}
